import { createStore } from 'vuex'
import charts from '@/store/charts'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    charts,
  }
})
